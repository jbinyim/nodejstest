extends base.pug

block content 
  h1 Change Title of video 
  form(method="POST")
    input(name="title" type="text" placeholder="whats your name" value=video.title required)
    input(type="submit" value="save")