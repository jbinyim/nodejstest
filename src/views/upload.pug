extends base.pug

block content
  if errorMessage
    span=errorMessage
  form(method="POST")
    input(name="title" type="text" placeholder="Title" maxLength=80 required)
    input(name="description" type="text" placeholder="description" minLength=20 required)
    input(name="hashtags" type="text" placeholder="hashtags, seperated by common" required)
    input(type="submit" value="upload video")